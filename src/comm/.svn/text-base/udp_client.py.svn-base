#!/usr/bin/env python
#-*- coding: utf8 -*-

from socket import *
import sys

if __name__ == "__main__":
	conn = socket(AF_INET, SOCK_DGRAM)
	conn.sendto(sys.argv[1], ("127.0.0.1", 9090))
	buf, addr = conn.recvfrom(1024)
	print buf
